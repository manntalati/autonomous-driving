torch==2.9.1
torchvision==0.24.1
opencv-python==4.12.0.88
pillow==12.1.1
albumentations==2.0.8
numpy==2.2.6
scipy==1.16.3
pycocotools==2.0.11
nuscenes-devkit
matplotlib==3.10.7
seaborn==0.13.2
tensorboard==2.20.0
ipykernel==6.29.5
PyYAML==6.0.2
omegaconf==2.3.0
tqdm==4.67.1
scikit-learn==1.7.2
pytest==9.0.2
streamlit>=1.30,<2.0

# Agentic Perception Platform (companion project)
# Pinned below 2.0: mcp 2.0.0 removed `mcp.server.fastmcp` and replaced it with
# a new `mcp.server.mcpserver` API. mcp_server/server.py is written against
# FastMCP, so an unpinned `mcp>=1.2.0` resolves to 2.0.0 in a fresh CI install and
# the server subprocess fails to import — which is what broke CI from 2026-07-30.
# Migrating to the 2.0 API is tracked separately; 1.29.0 is the latest 1.x.
mcp>=1.2.0,<2.0
anthropic>=0.40.0

# Datadog LLM Observability — traces the agent's Anthropic calls only.
# Inactive unless DD_LLMOBS_ENABLED=1 and DD_API_KEY are set (see .env.example).
ddtrace>=4.0